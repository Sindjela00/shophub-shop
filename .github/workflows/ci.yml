# CI pipeline placeholder — triggered on every PR against develop/master.
# TODO:
#   - build backend (dotnet build) and frontend (npm run build)
#   - run unit tests (dotnet test / vitest)
#   - run integration tests (Testcontainers)
#   - build & publish container image (semantic versioning) on merge
#   - require this pipeline to pass before a PR can be merged
name: CI
on:
  pull_request:
    branches: [develop, master]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
